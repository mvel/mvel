package org.drools.task.service;

public enum Operation {
    Claim,
    Start,
    Stop,
    Release,
    Suspend,
    Resume,
    Skip,
    Delegate,
    Forward,
    Complete,
    Fail;
}
